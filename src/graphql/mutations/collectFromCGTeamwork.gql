mutation collectFromCGTeamwork(
  $database: String!
  $prefix: String!
  $pipeline: String!
) {
  collectFromCGTeamwork(
    database: $database
    prefix: $prefix
    pipeline: $pipeline
  ) {
    originPrefix
    updatedCount
    createdCount
  }
}
